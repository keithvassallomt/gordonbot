# Vite env variables (exposed to client because of VITE_ prefix)
# Default backend base URL for API requests from the dashboard
VITE_API_BASE=http://192.168.96.187:8000

# Show debug buttons (Figure 8 Test, Debug Axes, Simulate Save)
VITE_DASHBOARD_DEBUG=false
